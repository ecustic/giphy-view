import styled from 'styled-components'
import { theme } from '../../theme'

export const TopNavigation = styled.main`
    padding: ${theme.layout.padding * 2}px;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 32px;
    font-weight: bold;
    background: ${theme.colors.primary};
    color: white;

`